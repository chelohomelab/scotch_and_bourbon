services:
  app:
    build: .
    container_name: scotch-and-bourbon
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./static/uploads:/app/static/uploads
    environment:
      - TZ=America/Chicago
